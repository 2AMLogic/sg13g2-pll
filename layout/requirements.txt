# Pinned klayout-tools (`klt`) install for this repo's device-level layout
# flows: layout/pll/ (SG13G2, issue #13) and layout/sg13cmos5l-pll/
# (SG13CMOS5L, issue #24). One pin serves both -- a second, deck-specific pin
# was considered for #24 and rejected, because two `klt` builds in one repo
# would make "which build produced this record" a per-record question rather
# than a repo-level fact.
#
# Pinned by exact git commit, mirroring 2AMLogic/sky130-pll's own
# layout/requirements.txt convention (see that repo's klt-pin-decision.md)
# and this repo's own CLAUDE.md harness-bootstrap rule: a commit hash names
# one immutable tree, independent of PyPI's own release cadence
# (klayout-tools#953, already tracked, cross-confirmed rather than re-filed).
#
# Bump history:
#
#   * 6d2028a32bfd385724498941572f3976783ae720 -- merged klayout-tools#1449
#     (closed #1448, "no sg13g2 entry in the analog-primitive generators'
#     PDK-family role-layer table"). Re-verifying end-to-end at that pin
#     found the role-layer table alone did not unblock drawing: every
#     `mos_array`/`diff_pair` request still hard-rejected `sg13g2`
#     (klayout-tools#1450, a real DRC rejection on the shared unit device's
#     gate-poly landing pad) and `res_array` only exposed the `"generic"`
#     (rsil) flavour, rejecting this design's `rppd`/`rhigh` resistors
#     (klayout-tools#1451). Net effect at that pin: 0/482 planned devices
#     drew.
#   * 5482cfe1c67eacf9d2f27d750a11a37ec14b1984 -- `main` HEAD as of that
#     bump, which includes both #1450's fix (PR #1453,
#     `747d06244391794ec84ea02036179fb2f2786caa`, a narrowed unit-device
#     gate-poly landing pad that clears `gatpoly.separation.activ.1`) and
#     #1451's fix (PR #1452, this commit itself, widening `res_array`'s
#     `sg13g2` flavour mechanism to a 3/4-slot `requires` set so
#     `rppd`/`rhigh` are selectable). Verified `747d06...` is an ancestor of
#     `5482cfe...` before pinning, so both fixes are present together. See
#     `layout/pll/README.md`'s friction log for the re-run transcripts this
#     bump produced.
#   * 04c0fa91221364e4983151358b05c135691c4d40 (current pin) -- `main` HEAD as
#     of this bump (issue #24), picking up two merged upstream fixes and
#     correcting one stale premise:
#
#     - **klayout-tools#1456** (PR, this commit itself): `klt extract` now
#       recognises `sg13g2`'s `cap_cmim`/`rfcmim` MIM capacitors. That closes
#       **klayout-tools#1454**, the deck-coverage gap *this repo itself filed*
#       from issue #13's own run, so the bump retires one of this repo's own
#       open friction entries rather than only importing someone else's.
#     - **klayout-tools#1458** (`74d4c0d`): `klt extract --pdk` binds
#       `sg13g2`'s `rsil`/`rppd`/`rhigh` to their real PDK subcircuits instead
#       of emitting bare `R` cards.
#     - **Stale premise, corrected here rather than inherited.** Issue #24 was
#       written on the understanding that the previous pin *predated*
#       SG13CMOS5L deck support and had to be bumped past
#       klayout-tools#1398's merge to get it. Re-verified before pinning, that
#       is **not** true: #1398 closed 2026-08-25, `5482cfe...` is dated
#       2026-08-29, and `git ls-tree 5482cfe -- src/klayout_tools/decks/`
#       already carried `sg13cmos5l.py`. `klt deck info --deck sg13cmos5l`
#       resolved at the old pin too. The bump is therefore worth making for
#       #1456/#1458, but it was never what unblocked SG13CMOS5L -- the real
#       SG13CMOS5L blocker is generator-side and is still open
#       (klayout-tools#1462; see `layout/sg13cmos5l-pll/README.md`).
#
#     Verified `5482cfe...` is an ancestor of `04c0fa9...` before pinning, so
#     nothing the previous pin carried is lost.
#   * fdf04f71ab39159838acb86e63a92d6fa0c714fa (current pin) -- `main` HEAD as
#     of this bump (issue #31), picking up the fix issue #24 deliberately did
#     not chase:
#
#     - **klayout-tools#1461** (PR, `25c52af27f5518959f8afa2e11c5449741886c9c`):
#       `klt gen cap_array` now has a real `sg13g2` MiM-capacitor plate-layer
#       configuration (`cap_top_plate`/`cap_bottom_plate`/`cap_top_via`/
#       `cap_top_via_metal`, transcribed from the `sg13g2` deck's own
#       `cap_cmim` extraction entry), closing **klayout-tools#1455** -- the
#       gap this repo itself filed from issue #13's own run and the last
#       thing standing between the SG13G2 record and 482/482. Verified
#       `25c52af...` is an ancestor of `fdf04f71...` before pinning
#       (`gh api .../compare/25c52af...fdf04f71` reports `"status": "ahead"`,
#       5 commits, 0 behind), so the fix is present at this pin.
#     - Also picks up **klayout-tools#1465** (`b10fa3c6e`, closes #1462): adds
#       an `sg13cmos5l` role-layer entry so `klt gen mos_array`/`res_array`
#       resolve that family too -- relevant to `layout/sg13cmos5l-pll/`'s own
#       non-regression re-run, not this design's SG13G2 side.
#     - **What this bump does NOT pick up**: klayout-tools#1466 (new
#       device-recognition/extraction machinery for `cap_cmomi`/`cap_cmomf`,
#       SG13CMOS5L's own MoM capacitors) is filed but not merged as of this
#       pin -- `fdf04f71a` (`#1469`) only documents that gap, it does not
#       close it. The SG13CMOS5L side's two MoM-capacitor devices are
#       therefore still expected to be undrawable at this pin; see
#       `layout/sg13cmos5l-pll/README.md`.
#
# Bump discipline, mirrored from sky130-pll: pin an exact commit, never
# floating `main`; state in this header what the bump picks up; and re-run
# **both** `layout/bin/run-pll-layout-flow.sh` and
# `layout/bin/run-pll-cmos5l-layout-flow.sh`, checking the refreshed records
# in as the non-regression (or, hopefully, regression-fixed) proof.
klayout-tools @ git+https://github.com/2AMLogic/klayout-tools@fdf04f71ab39159838acb86e63a92d6fa0c714fa
