# Corner matrix — `sg13cmos5l-loop-filter-momcap`

See `../records/RECORD-001-rc-corner-momcap-sensitivity.md` "Corner matrix"
for the full axis-by-axis rationale (including why supply is not swept for
this DUT). Summary:

| Axis | Values |
|---|---|
| Process (R1 / rppd only) | `res_typ`, `res_bcs`, `res_wcs` |
| Temperature | -40C, 27C, 125C |
| MOM-cap uncertainty (C1, C2, uniform) | -20%, 0%, +20% |

27 rows total. Raw results: `results.csv`
(`res_corner,temp_c,mom_frac,r1_ohm,c1_f,c2_f,fz_hz,fp_hz`), generated by
`../testbench/run.sh`.

**Resized R1 (issue #41, DR-006)**: the same 27-row matrix, re-run with
`XR1` at its post-resize geometry (`w=0.6u l=810u`, vs. `w=4u l=120u`
as-drawn) — `C1`/`C2` untouched. Raw results: `results_resized.csv`, same
column layout, generated by the same `../testbench/run.sh` (now runs both
passes). See `../records/RECORD-002-r1-resize-momcap.md`.
